import type { NextPage } from "next";
import WhoToFollowScreen from "@/screens/WhoToFollowScreen";

const WhoToFollow: NextPage = () => {
    return <WhoToFollowScreen />;
};

export default WhoToFollow;
